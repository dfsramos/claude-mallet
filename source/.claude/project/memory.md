# Project Memory

## Commands & Access Patterns

<!-- Example entries — replace or remove these:
- Run dev server: `npm run dev` (starts on :3000, requires .env.local)
- Run migrations: `npx prisma migrate dev` (never against prod without explicit confirmation)
- Run full test suite: `npm test -- --runInBand` (parallel runs fail due to shared DB)
-->

## Conventions

<!-- Example entries — replace or remove these:
- Feature branches: `f/<description>`, bug branches: `b/<description>`
- API handlers live in `src/routes/`, not `src/api/` (legacy naming, do not create new files under `src/api/`)
- All DB queries go through the repository layer in `src/repositories/`
-->

## Gotchas

<!-- Example entries — replace or remove these:
- The `users` table has a soft-delete column (`deleted_at`); always add `WHERE deleted_at IS NULL` to queries
- `config/production.json` is gitignored — it must be created manually from `config/production.example.json`
-->

## Preferences

<!-- Example entries — replace or remove these:
- Prefer `tsx` over `ts-node` for running TypeScript scripts directly
- Use `pnpm` — `npm` is present but not the team standard
-->
